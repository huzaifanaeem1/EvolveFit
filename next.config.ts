import type { NextConfig } from "next";


module.exports = {
  images: {
    domains: ['images.unsplash.com', 'encrypted-tbn0.gstatic.com', 'www.mensjournal.com', 'img.freepik.com'],
  }
}
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
